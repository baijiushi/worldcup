# 世界杯竞猜网站

这是从 `wroldcup.txt` 还原出来的 React + Vite 网页项目。

## 本地运行

```powershell
npm install
npm run dev
```

打开：

```text
http://localhost:5173/
```

## 打包

```powershell
npm run build
```

打包后的静态文件会生成在 `dist/` 目录。
